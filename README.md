I want to convert my anki cards into a wallpaper ready format. I use manim since they contain latex code.

The '''media/''' directory is used as the default location to save files by manim. The '''out/''' directory is used to store temporary files, before postprocessing. In '''collection/''' the finished scenes are stored.

This doesnt support nested environments.
